<!DOCTYPE html>
<html lang='en'>
<head>
    <meta charset='UTF-8'>
    <meta http-equiv='X-UA-Compatible' content='IE=edge'>
    <meta name='viewport' content='width=device-width, initial-scale=1.0'>
    <%- include('../partials/head'); %>

    <title>About</title>
</head>

<body>
    <header>
        <%- include('../partials/header'); %>
    </header>

    <main class='explore-main'>
        <h1>Explore</h1>
        <p>Find new people to follow!</p>

        

        <div id='profiles'>
            <% profiles.forEach((profile) => { %>
              <div class="<%= profile.subId %>">
                <h2><%= profile.name %></h2>
                <p>Age: <%= profile.age %></p>
                <p><%= profile.songs %></p>
                <p><%= profile.about %></p>
                <form action="/follow/<%= profile.subId %>" data-profile-id= "<%= profile.subId %>" method="POST">
                  <input type="hidden" name="followStatus" value="<%= profile.follow ? 'false' : 'true' %>">
                  
                  <% if (profile.follow) { %>
                    <button>Unfollow</button>
                  <% } else { %>
                    <button>Follow</button>
                  <% } %>
                </form>
                <img src='<%= profile.image %>'>
              </div>
            <% }); %>
          </div>

    
    </main>

    <footer>
        <%- include('../partials/footer'); %>
    </footer>
    <script src='/scripts/navbarScript.js'></script>
    <script src='/scripts/exploreConfirmButtonScript.js'></script>
</body>
</html>







